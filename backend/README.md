# Keskonmange Backend

## Dev Setup

Note: you can also use the Docker Compose project at the root if you prefer (database + backend setup).

- Setup the database
- Edit the environment variables in .env
```
npm i
npm run dev
```

## Prod Setup

Use the Dockerfile and the Docker Compose / other orchestration tool.